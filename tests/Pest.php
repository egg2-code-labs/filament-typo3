<?php

use Egg2CodeLabs\FilamentTypo3\Tests\TestCase;

uses(TestCase::class)
    ->in(__DIR__)
    ->beforeEach(function (): void {
        // Standard setup for all tests
    })
    ->afterEach(function (): void {
        // Cleanup after each test
    });
